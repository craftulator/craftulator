- Context: Theme logo images should be owned by the future logo display component rather than published as generic
  public assets.
- Decision: Keep theme logo images in `assets/themes/images/` for now and import them explicitly when the logo display
  component is implemented. Component-owned images should live in a component-local `images/` directory.
- Consequences: Milestone 2 no longer publishes theme logos under `public/`; Milestone 4 will handle logo display and
  explicit image imports alongside the shared app foundation work.
- Status: accepted
