# Task Discussion Log

- The user asked to add project rules for small logical commits, kebab-case code filenames, type-only imports, FSD-like
  architecture, colocated tests, and component structure.
- The user clarified that app routes must import pages from `@pages`.
- The user clarified that `src/pages` mirrors URLs and only re-exports code from `src/templates`.
- The user asked to document all of these rules in agent instructions and project documentation.
