# Task Discussion Log

- The user asked to add `vite-tsconfig-paths` and configure it.
- The implementation should keep exact dependency versions and continue using `tsconfig.json` aliases as the source of
  truth.
