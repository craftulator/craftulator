- Context: Agents may encounter tasks where an existing package is a better fit than custom implementation.
- Decision: Instruct agents to propose installing a suitable package when one exists, while still requiring explicit
  user permission before adding any dependency.
- Consequences: Future agents should surface appropriate dependency options earlier, but dependency additions remain
  controlled by the existing approval and exact-version policy.
- Status: accepted
