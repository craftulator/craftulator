- Context: The project already targets Node.js 24, but local version managers need an exact pinned version.
- Decision: Add `.nvmrc` with `v24.16.0` and configure CI to read the Node.js version from that file.
- Consequences: Local `nvm use` and GitHub Actions now resolve the same Node.js 24 patch version, while `package.json`
  keeps the broader `24.x` engine range for developer compatibility.
- Status: accepted
