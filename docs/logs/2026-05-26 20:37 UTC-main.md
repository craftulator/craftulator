# Task Discussion Log

- The user asked to remove theme logos from `public/` because the app will later import them explicitly from the image
  source directory in the component responsible for logo display.
- `public/theme-logos/` was removed while keeping the source images in `assets/themes/images/`.
- The branding test now verifies that theme logos remain source-only and are not published from `public/theme-logos/`.
- The roadmap was updated to defer explicit theme logo imports to the future logo display component.
- Component structure documentation now includes an `images/` directory for component-owned images.
