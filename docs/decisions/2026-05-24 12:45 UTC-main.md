- Context: The repository is being converted from an npm library boilerplate into an application template for React,
  React Router DOM, and React Intl.
- Decision: Use Vite as the application build tool, keep the template private, remove npm publishing configuration, and
  commit verified changes to the current branch.
- Consequences: The project now optimizes for local app development, routed React pages, localized messages, browser
  builds, and current-branch agent workflows instead of package publication.
- Status: accepted
