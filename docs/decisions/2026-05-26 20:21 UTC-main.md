- Context: Milestone 1 requires Craftulator project foundation updates without starting page implementation work.
- Decision: Keep the current React/Vite structure, update package and README metadata to Craftulator, configure Vite
  with `base: '/'`, and add focused configuration tests for package metadata and root-path deployment.
- Consequences: Future milestone work starts from Craftulator-specific metadata and a root-path GitHub Pages build
  target. The repository-hosting item remains open until the remote or hosted repository can be confirmed under the
  `craftulator` organization.
- Status: accepted
